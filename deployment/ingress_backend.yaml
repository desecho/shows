---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shows-backend
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - api.shows.samarchyan.me
      secretName: shows-backend-tls
  rules:
    - host: api.shows.samarchyan.me
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: shows-backend
                port:
                  number: 80
